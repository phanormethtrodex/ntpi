#!/bin/bash
#ntp auto-recon+synchro(.NIST.uni)->decon+self-synch/dejitter(.LOCL.inf)
sudo bash /home/pi/bin/rentp.sh && bash /home/pi/bin/ntpz.sh || echo "ntp synch fail..";
#watch -n1 ntpq -np
#ntpq -np
#bash /home/pi/bin/ntpz.sh;
